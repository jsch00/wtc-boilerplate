# Scope — {{COLLECTION}}

<!-- Template: harness/collection-SCOPE.md, seeded into <collection>/WTC-SCOPE.md.
     Local and ephemeral: this file dies with the collection (retire.sh).
     Durable task state — decisions, PR links — belongs in the issue and the
     PR. Keep this short enough that an agent reads all of it before starting
     work. Widening the scope is a deliberate edit; see
     harness/instructions/collection-context.md. -->

## Task

(One paragraph: what this collection exists to do, and what "done" looks like.
 Seeded from HANDOFF.md by /wtc-start.)

## Tracking

- Issue: `(id, and tracker key if there is one)`
- Branch: `(<issue-id>-<slug>, created at the first commit)`
- PRs: `(links, once open)`

## In scope

{{REPOS}}

## Out of scope

- (What a reasonable agent might touch next, and should not. Name the
  collection or repo that owns it instead.)

## Working notes

- (Short-lived context for the next session in this collection. Anything worth
  keeping after retire goes in the issue or the PR.)
